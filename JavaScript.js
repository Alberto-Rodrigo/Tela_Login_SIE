
    const botao = document.getElementById("btnClique");

    botao.addEventListener("click", loginSucesso);

    function loginSucesso(){
        alert("Login feito com sucesso!");
        return;
    }
